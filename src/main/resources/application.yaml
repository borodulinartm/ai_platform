spring:
  application:
    name: freshrss_news_upload
  datasource:
    url: jdbc:postgresql://183.87.45.129:5432/freshrss
    username: freshrss
    password: freshrss
    driver-class-name: org.postgresql.Driver
  ai:
    openai:
      chat:
        options:
          temperature: 1
          model: glm-5
      base-url: https://api.modelarts-maas.com
      api-key: 32C_du4rbvFjq_j3uXHIrZQ6FXwyDIWDsm_a_X2zacPf5c9cA9vpnLFeLrrduuAEBjKSNxR2xK4yhjZ-037Pjw

server:
  servlet:
    context-path: /api
  port: 8888

mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml

cloud:
  directories:
    category: category
    feed: feed
    articles: articles
    news_summary: news_summary

  file-name: report.json
  base-path-files: ${BASE_PATH_CLOUD}
